# sentiment analysis api

this project is a simple api built with fastapi and langchain to analyze the sentiment of user text. it is containerized using docker for easy deployment.

## how to run locally

1. install dependencies:
   ```bash
   pip install -r requirements.txt